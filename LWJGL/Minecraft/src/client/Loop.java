package client;
public class Loop
{
	public static void run()
	{
		// GL.createCapabilities();
		//
		// glClearColor(1.0f, 0.0f, 0.0f, 0.0f);
		//
		// long lTime = System.currentTimeMillis();
		// while (glfwWindowShouldClose(Main.window) == GLFW_FALSE)
		// {
		// // Control.getInputs();
		// update();
		// long t = System.currentTimeMillis() - lTime;
		// if (t < Main.TIME_STEP)
		// {
		// try
		// {
		// Thread.sleep((long) (Main.TIME_STEP - t));
		// }
		// catch (InterruptedException e)
		// {
		// e.printStackTrace();
		// }
		// }
		// Render.run();
		// lTime = System.currentTimeMillis();
		// }
	}

	public static void update()
	{
		System.out.println("Updating");
	}
}
